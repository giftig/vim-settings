" FZF install location on macs with homebrew
set rtp+=/usr/local/opt/fzf

" The preview window inevitably obscures the most important part of the
" filename when working with scala projects
let g:fzf_preview_window = ''
